# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config
.claude/

# Local env files — never commit these. The repo is public, and although the
# only value here today is the Supabase anon key (public by design), an
# untracked-by-default .env means the next secret added can't leak by accident.
.env
.env.*
!.env.example
